<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- windowBackground задан цветом, а не оставлен по умолчанию: иначе на холодном
         старте и при смене темы внутри приложения под Compose на миг просвечивает
         чужой фон окна. Системные бары прозрачны, чтобы edge-to-edge был стабилен
         с первого кадра и контент не прыгал от поздних window insets. -->
    <style name="Theme.Cryptocompare" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
